from django.apps import AppConfig


class HordeConfig(AppConfig):
    """
    Configuration class for the horde application
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'horde'
